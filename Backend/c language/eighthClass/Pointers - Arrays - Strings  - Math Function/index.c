#include <stdio.h>
#include <math.h>

int main()
{
    // To find square root of a number
    // printf("%f\n", sqrt(16));

    // To find the power of a number
    // printf("%f\n", pow(4, 2));

    // To Ceil the number
    // printf("%f\n", ceil(1.4));

    // To Floor the number
    // printf("%f\n", floor(1.4));

    // Pointers

    // int age = 43;

    // int *ptr = &age;

    // printf("%p\n", &age);

    // printf("%p\n", ptr);

    // printf("%d\n", *ptr);

    // *****************************

    // Arrays

    // int matrix[3] = {{23, 45, 68}};
    // printf("%lu", sizeof(matrix));

    // Strings

    char greating[] = "Hello";
    char adnan[] = "Adnan";

    // adnan[0] = 'j';
    printf("%s %s",greating, adnan);
}